import { useSidebar } from "../ui/sidebar";

const Header: React.FC = () => {
  return (
    <header className={"p-5 transition-margin duration-300"}>
      <div>
        
      </div>
    </header>
  );
};

export default Header;